server:
  port: 8082
  servlet:
    # 解决使用flux流式返回乱码
    encoding:
      enabled: true
      force: true
      charset: UTF-8


spring:
  application:
    name: tool-calling
  ai:
    dashscope:
      api-key: ${AI_DASHSCOPE_API_KEY}
    alibaba:
      toolcalling:
        time:
          enabled: true
