server:
  port: 8088
  servlet:
    # 解决使用flux流式返回乱码
    encoding:
      enabled: true
      force: true
      charset: UTF-8

spring:
  application:
    name: chatMemory4InMemory
  ai:
    dashscope:
      api-key: ${AI_DASHSCOPE_API_KEY}
